# Copilot Instructions

## Project Guidelines
- User preference for dotnet-version-upgrade: Only update NuGet packages and target framework versions. Do NOT make any code changes (no API fixes, no source incompatibility fixes).